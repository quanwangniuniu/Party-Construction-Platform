<Window x:Class="Niuniumama.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Loading" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Icon="./Static/Icons/running_icon.png"
        Background="Transparent">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <!-- 背景图片 -->
        <Image Source="./Static/Images/loading_picture.png" Stretch="Fill" Opacity="0.8"/>
        <Grid Background="White" Opacity="0.9" Margin="20" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <!-- 企业Logo -->
                <Image Source="./Static/Images/loading_logo.png" Height="150" Margin="0,0,0,20"/>
                <!-- 加载文字 -->
                <TextBlock Text="正在加载..." FontSize="24" FontWeight="Bold" Foreground="#007ACC" Margin="0,0,0,20"/>
                <!-- 进度条 -->
                <Grid Background="White" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ProgressBar Name="ProgressBar" Width="300" Height="30" Margin="20"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</Window>